{
 "CORRECTION": {
  "so_the_premise_does_not_survive_routing": "the audit measured the conflict cone at 16.10 ns and named it the control plane's clock, but that is a PRE-LAYOUT synth+sta number.  Routed, the cone is about 1.67 ns at BOTH depths -- the same discipline that had the microsequencer at 8.18 MHz pre-layout and 317 MHz routed.  The dependence table is not the routed limiter, so splitting it cannot be the lever.",
  "what_I_reported": "that a closed routed record at 603.7 MHz removes the audit's stated reason for refusing its 2.00x sustained sizing",
  "what_is_still_true": "the split is proven equivalent end to end, costs nothing in area, and is available behind a parameter.  What is NOT true is that it buys 2.00x sustained throughput.",
  "what_the_second_record_shows": "the SAME block at CHECK_STAGES=1 closes at 599.9 MHz.  The split buys 0.6%, and depth 2 is slightly SMALLER (149,194 cells against 153,635) because a registered reduction needs less repair than a combinational one.",
  "where_to_look_instead": "the FULL 12-module sequencer routes at 264.8 MHz with **3,204 max-slew violations** and 3 max-cap.  Thousands of slew violations is a drive and fan-out signature, not a depth one -- a badly-slow ASAP7 block here has usually been ONE net with hundreds of loads.  The full sequencer at depth 2 is routing now, and if it lands near 264.8 MHz with the violations intact that settles it: the lever is the fan-out, and the cone was a red herring that only a routed record could expose."
 },
 "and_the_consumer_is_proven_not_assumed": {
  "result": {
   "depth_1": {
    "iverilog": "pass",
    "verilator": "pass"
   },
   "depth_2": {
    "iverilog": "pass",
    "verilator": "pass"
   },
   "marker": "PASS: ABI3 RTL microsequencer cases=65 headers=65 programs=53 issues=185 views=460 traps=11",
   "reading": "the same 65 cases, the same 53 programs, the same 185 engine-issue events and the same 11 traps at depth 2.  The deeper form takes more cycles to do it, which is the whole trade: the cycle buys the clock."
  },
  "the_change": "ot_a3_microsequencer takes DEP_CHECK_STAGES and owes the table one more cycle at depth 2, in BOTH consumers -- the range scan and the predicate hazard.  The owed count is a comparison rather than a part-select of the integer parameter, because the depth campaign records that indexing a part-select is accepted by one of the two simulators and rejected by the other.",
  "the_gate": "tools/rtl_abi3_dep_check_depth_campaign.py replays the WHOLE ABI 3.0 vector set through the real control plane at both depths under both simulators, and requires the marker the vector set derives from its golden execution on runtime.sim.device.Device",
  "what_was_missing": "rtl/test/tb_a3_dependence_table_depth.sv proved the TABLE: depth 2 equals depth 1 delayed by one cycle, 20,000 cycles, both simulators.  It says nothing about the microsequencer, whose hazard scan is pipelined -- each cycle issues a check pair and folds in the previous cycle's results -- so at depth 2 the last pair's answer arrives after the loop has left.  Dropping it would issue an instruction over a live hazard: a silent wrong answer, not a stall."
 },
 "answer": "yes -- and the same record at depth 1 closes at 599.9 MHz, which REFUTES the sizing the record was meant to unlock.  See CORRECTION.",
 "default_is_still_1": "the parameter defaults to 1 in both the table and the sequencer, so the shipped elaboration and every bound vector are unmoved.  Adopting depth 2 is now a one-parameter decision with both halves of its evidence in hand.",
 "not_a_claim": [
  "the 2.152x sustained figure is the AUDIT's sizing, not a measurement of this record; what this record removes is the audit's stated reason for refusing it",
  "the depth-1 routed record is still running, so the frequency RATIO between the two depths is not stated here"
 ],
 "question": "results/derived/abi3_iso_area_three_level_audit.json named ONE combinational cone as the control plane's clock and sized splitting it at 2.00x sustained throughput, while refusing to call that a result 'until a closed routed record exists at 534 MHz or better'.  Does one exist?",
 "schema": "opentallas.result.dep_check_depth_closes.v1",
 "the_routed_record": {
  "against_the_bar": "603.7 MHz against the audit's 534 MHz, so the bar is met",
  "antenna": 0,
  "cells": 149194,
  "closed": true,
  "core_area_um2": 47629.1,
  "drc": 0,
  "fmax_mhz": 603.678,
  "hold_wns_ns": 0.0526,
  "module": "ot_a3_dependence_table",
  "parameters": {
   "CHECK_STAGES": 2
  },
  "record": "results/physical_abi3/asap7/dependence_table/pnr_1p8ns_check_stages_2_closed.json",
  "standard_cell_area_um2": 18606.5,
  "status": "pass",
  "target_period_ns": 1.8
 },
 "the_routed_records": {
  "check_stages_1": {
   "area_um2": 19258.9,
   "cells": 153635,
   "closed": true,
   "fmax_mhz": 599.9,
   "max_slew_violations": 0
  },
  "check_stages_2": {
   "area_um2": 18606.5,
   "cells": 149194,
   "closed": true,
   "fmax_mhz": 603.678,
   "max_slew_violations": 0
  },
  "ratio": 1.006
 }
}
